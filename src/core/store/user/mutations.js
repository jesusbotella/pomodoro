/* eslint-disable no-param-reassign */
export function setUser(state, user) {
  state.user = user;
}

export function setAuthentication(state, authenticationData) {
  state.authentication = authenticationData;
}
